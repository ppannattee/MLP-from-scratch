# MLP from Scratch

This repository contains an implementation of a Multi-Layer Perceptron (MLP) for MNIST digit classification from scratch, **without using PyTorch built-in layers or autograd**.

The notebook also provides detailed, step-by-step mathematical explanations for both the forward and backward passes. It is designed to help readers understand how neural networks work internally.